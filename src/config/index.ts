import {chmodSync,mkdirSync} from 'node:fs';import {homedir} from 'node:os';import {isAbsolute,join,resolve} from 'node:path';import {getAddress} from 'viem';import {z} from 'zod';import {NOXA_FACTORY_ADDRESS,NOXA_FACTORY_START_BLOCK} from '../noxa.js';
const boolean=z.enum(['true','false']).transform(v=>v==='true');const url=z.string().url().refine(v=>['http:','https:'].includes(new URL(v).protocol),'must be an HTTP(S) URL');
const schema=z.object({NODE_ENV:z.enum(['development','test','production']).default('development'),DATA_DIR:z.string().min(1).optional(),XDG_STATE_HOME:z.string().optional(),NETWORK:z.enum(['testnet','mainnet']).default('testnet'),MAINNET_ENABLED:boolean.default('false'),MAINNET_ACKNOWLEDGE_RISK:z.string().optional(),EXECUTION_MODE:z.literal('read-only').default('read-only'),HOST:z.string().default('127.0.0.1'),PORT:z.coerce.number().int().min(1).max(65535).default(8787),LOG_LEVEL:z.enum(['debug','info','warn','error']).default('info'),SHUTDOWN_TIMEOUT_MS:z.coerce.number().int().positive().default(30000),UNSAFE_ALLOW_EXTERNAL_DATABASES:boolean.default('false'),RPC_URL:url.optional(),CHAIN_ID:z.coerce.number().int().positive().optional(),NOXA_FACTORY_ADDRESS:z.string().optional(),NOXA_FACTORY_START_BLOCK:z.coerce.bigint().nonnegative().optional(),MARKET_PROVIDER_URLS:z.string().optional(),API_BEARER_TOKEN:z.string().min(1).optional(),API_BEARER_TOKEN_SHA256:z.string().regex(/^[a-fA-F0-9]{64}$/).optional(),API_TENANT_ID:z.string().min(1).default('default'),API_SCOPES:z.string().default('agent:read,tool:read')}).passthrough();
export interface AppConfig{mode:'development'|'test'|'production';network:'testnet'|'mainnet';execution:'read-only';host:string;port:number;logLevel:string;shutdownTimeoutMs:number;dataDir:string;auth:{bearerToken?:string;bearerTokenSha256?:string;tenantId:string;scopes:string[]};rpc?:{url:string;chainId:number};noxa:{factoryAddress:`0x${string}`;startBlock:bigint};marketProviderUrls:string[];paths:{sessions:string;runs:string;jobs:string;events:string;triggers:string;indexer:string;memory:string;skills:string;logs:string;audit:string}}
export function loadConfig(env:Record<string,string|undefined>=process.env,cwd=process.cwd(),requirements:{requireRpc?:boolean}={}):AppConfig{const e=schema.parse(env);if(requirements.requireRpc&&!e.RPC_URL)throw new Error('RPC_URL is required');const data=e.DATA_DIR??join(e.XDG_STATE_HOME??join(homedir(),'.local','state'),'robinhood-agent-os');if(!isAbsolute(data))throw new Error('DATA_DIR must be an absolute path');if(e.NETWORK==='mainnet'&&!(e.MAINNET_ENABLED&&e.MAINNET_ACKNOWLEDGE_RISK==='I_ACKNOWLEDGE_MAINNET_RISK'))throw new Error('Mainnet requires double opt-in: MAINNET_ENABLED and MAINNET_ACKNOWLEDGE_RISK');const expected=e.NETWORK==='mainnet'?4663:46630;const chainId=e.CHAIN_ID??expected;if(chainId!==expected)throw new Error(`CHAIN_ID does not match ${e.NETWORK}`);let factoryAddress:`0x${string}`;try{factoryAddress=getAddress(e.NOXA_FACTORY_ADDRESS??NOXA_FACTORY_ADDRESS)}catch{throw new Error('NOXA_FACTORY_ADDRESS is invalid')}const marketProviderUrls=(e.MARKET_PROVIDER_URLS?.split(',').filter(Boolean)??[]).map(v=>url.parse(v));const root=resolve(data);mkdirSync(root,{recursive:true,mode:0o700});chmodSync(root,0o700);for(const child of ['memory','skills','logs','audit'])mkdirSync(join(root,child),{recursive:true,mode:0o700});return{mode:e.NODE_ENV,network:e.NETWORK,execution:e.EXECUTION_MODE,host:e.HOST,port:e.PORT,logLevel:e.LOG_LEVEL,shutdownTimeoutMs:e.SHUTDOWN_TIMEOUT_MS,dataDir:root,auth:{...(e.API_BEARER_TOKEN?{bearerToken:e.API_BEARER_TOKEN}:{}),...(e.API_BEARER_TOKEN_SHA256?{bearerTokenSha256:e.API_BEARER_TOKEN_SHA256.toLowerCase()}:{}),tenantId:e.API_TENANT_ID,scopes:e.API_SCOPES.split(',').map(x=>x.trim()).filter(Boolean)},...(e.RPC_URL?{rpc:{url:e.RPC_URL,chainId}}:{}),noxa:{factoryAddress,startBlock:e.NOXA_FACTORY_START_BLOCK??NOXA_FACTORY_START_BLOCK},marketProviderUrls,paths:{sessions:join(root,'sessions.sqlite'),runs:join(root,'runs.sqlite'),jobs:join(root,'jobs.sqlite'),events:join(root,'events.sqlite'),triggers:join(root,'triggers.sqlite'),indexer:join(root,'indexer.sqlite'),memory:join(root,'memory'),skills:join(root,'skills'),logs:join(root,'logs'),audit:join(root,'audit')}}}
export function sanitizedConfig(c:AppConfig){return{mode:c.mode,network:c.network,execution:c.execution,host:c.host,port:c.port,dataDir:c.dataDir,logLevel:c.logLevel,rpcConfigured:!!c.rpc,marketProvidersConfigured:c.marketProviderUrls.length,noxa:{factoryConfigured:true,startBlock:c.noxa.startBlock.toString()}}}
