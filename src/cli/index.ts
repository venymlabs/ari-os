export type CliCommand={name:"chat";message:string;sessionId?:string}|{name:"simulate";input:unknown}|{name:"status"|"sessions"|"tools"|"skills"|"markets"|"jobs"};
export interface CliServices{chat(x:{message:string;sessionId?:string}):unknown;status():unknown;sessions():unknown;tools():unknown;skills():unknown;markets():unknown;simulate(x:unknown):unknown;jobs():unknown}
const simple=new Set(["status","sessions","tools","skills","markets","jobs"]);
export function parseCommand(argv:string[]):CliCommand{const [name,...args]=argv;if(!name)throw new Error("Command required");if(simple.has(name)){if(args.length)throw new Error(`Unexpected arguments for ${name}`);return{name:name as "status"}}if(name==="simulate"){if(args.length!==1)throw new Error("simulate requires one JSON argument");try{return{name,input:JSON.parse(args[0]!)}}catch{throw new Error("Invalid simulation JSON")}}if(name==="chat"){let sessionId:string|undefined;const at=args.indexOf("--session");if(at>=0){sessionId=args[at+1];if(!sessionId||at!==args.length-2)throw new Error("Invalid --session option");args.splice(at,2)}const message=args.join(" ").trim();if(!message)throw new Error("chat message required");return{name,message,...(sessionId===undefined?{}:{sessionId})}}throw new Error(`Unknown command: ${name}`)}
const safeError=(e:unknown)=>({error:e instanceof Error?e.message.replace(/(?:token|secret|key|password)\s*[=:]\s*\S+/gi,"[redacted]"):"Command failed"});
export async function runCli(argv:string[],services:CliServices,write:(text:string)=>void=(x)=>process.stdout.write(x+"\n")){try{const c=parseCommand(argv);let result:unknown;if(c.name==="chat")result=await services.chat({message:c.message,...(c.sessionId===undefined?{}:{sessionId:c.sessionId})});else if(c.name==="simulate")result=await services.simulate(c.input);else result=await services[c.name]();write(JSON.stringify({ok:true,result}));return 0}catch(e){write(JSON.stringify({ok:false,...safeError(e)}));return 1}}
